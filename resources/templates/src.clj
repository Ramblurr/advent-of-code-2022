(ns aoc.{{year}}.day{{day}}
    (:require
     [aoc.core :as core :refer [read-input read-input-lines]]))

(def sample (read-input "{{year}}/day{{day}}-sample.txt"))
(def input (read-input "{{year}}/day{{day}}.txt"))

(defn part-1 [input] true)

(defn part-2 [input] true)
